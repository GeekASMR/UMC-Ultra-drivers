- 兼容原厂双通道并全系接管 PreSonus 宿主。
- 修复新电脑上安装后 ASIO 驱动不可见的问题（缺失 libusb 依赖 + System32 部署被拦截）。
- 修复虚拟通道热切换（如 V12 和 V34）后无声的问题：当 WDM 内核驱动重建共享内存段时，ASIO 端自动检测并重连。
- BehringerASIO.dll 改为静态链接 CRT（/MT），新系统无需安装 VC++ 运行库。
